# Optional metadata for Wrangler CLI (Pages dashboard can build without this file).
# https://developers.cloudflare.com/workers/wrangler/configuration/

name = "nexperts-website"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# Optional: for `wrangler pages dev` — create KV in dashboard, paste namespace id below.
# Pages Git deploys: bind COURSE_OVERRIDES in the Pages project UI (Settings → Functions).
# [[kv_namespaces]]
# binding = "COURSE_OVERRIDES"
# id = "YOUR_KV_NAMESPACE_ID"
